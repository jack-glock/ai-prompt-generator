# QUALITY_SCORE — 품질 루브릭

> 무엇이 "충분히 좋은가"를 정의한다. 새 PR·커밋 전에 이 표를 통과해야 한다.

## 1. 코드 품질

| 항목 | 합격선 |
|---|---|
| TypeScript 컴파일 에러 | 0개 (`npx tsc --noEmit` 통과) |
| ESLint 경고 | 0개 (Next.js 기본 룰셋 기준) |
| `console.log` 잔존 | 0개 (디버그 흔적 제거) |
| 한글 주석 | 컴맹 사용자 전제. 비기술 용어 우선. |
| 단일 함수 길이 | 가능하면 80줄 이하. 넘으면 헬퍼 분리 검토. |
| `any` 타입 | 가능하면 0개. 불가피하면 주석으로 사유 기록. |

## 2. 사용자 경험

| 항목 | 합격선 |
|---|---|
| 입력 후 결과 갱신 지연 | <100ms (`useMemo` 실시간 미리보기) |
| 복사 버튼 피드백 | 1.5초 동안 "복사됨" 표시 후 원상복귀 |
| 첫 화면 로딩 시간 | 모바일 LTE에서 3초 이내 |
| 한글 입력 깨짐 | 0건 (UTF-8 강제) |
| 모든 버튼 한국어 라벨 | 100% |

## 3. 프롬프트 품질

| 항목 | 합격선 |
|---|---|
| GPT Image 출력 | 자연 영어 문장 5~7개. 마침표 포함. |
| Nano Banana 출력 | `Keep:` `Change:` `Remove:` 3개 블록 모두 존재 |
| Midjourney 출력 | 키워드 콤마 구분 + 끝에 `--ar W:H` |
| 한글 요청 보존 | 자유 문장은 영어 변환 없이 따옴표로 인용 |
| 영어 부분 문법 | 어색해도 작동에 영향 없으면 OK |

## 4. 문서 품질

| 항목 | 합격선 |
|---|---|
| `AGENTS.md` 길이 | 200줄 이하 (안내판 역할 유지) |
| 코드↔문서 동기화 | 새 기능은 `FEATURES.md`에 즉시 반영 |
| 날짜 형식 | **YY-MM-DD** (예: 26-04-28) — 이 프로젝트 한정 |
| 톤 | 공식적, 컴맹 사용자 친화적 |
| 의도적 미구현 사유 | `BACKLOG.md`에 명시 |

## 5. 배포 안정성

| 항목 | 합격선 |
|---|---|
| Vercel 빌드 시간 | 90초 이내 |
| Vercel 빌드 실패 | 0건 (push 전 로컬 `npm run build` 권장) |
| 라이브 사이트 응답 | 200 OK |
| `.gitignore` 누락 | 0건 (`.env*`, `node_modules/`, `.next/`, `.devsync/` 등) |

## 6. 자가 점검 (커밋 전)

- [ ] `npm run build` 무경고 통과
- [ ] 새 옵션 추가했다면 `FEATURES.md` 갱신
- [ ] 새 의존성 추가했다면 `SECURITY.md` §3 점검
- [ ] 디자인 변경했다면 `UI_DESIGN.md` 갱신
- [ ] 프롬프트 규칙 변경했다면 `PROMPT_STRATEGY.md` 갱신
- [ ] 의도적으로 안 한 일이 있다면 `BACKLOG.md`에 사유 추가
