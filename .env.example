# 이 파일은 환경변수 예시입니다 (git에 올라가도 안전한 빈 템플릿).
# 실제 값은 같은 폴더의 `.env.local` 파일에 적어 주세요 (`.env.local`은 .gitignore에 의해 자동으로 제외됩니다).

# Google AI Studio에서 발급받은 Gemini API 키
# 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=
