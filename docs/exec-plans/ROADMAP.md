# ROADMAP — 실행 계획

> 다음에 할 일. 우선순위 + 사유. 완료된 항목은 즉시 제거하고 `FEATURES.md`로 옮긴다.
>
> 마지막 갱신: 26-04-28 (v0.4 라이브 반영)

## 완료 (참고용 이력)

- ✅ **v0.3 (26-04-28)** — 모델 12종 분기 빌더 + 영어 textarea 입력 분리. 자동 번역 미채택 결정.
- ✅ **v0.4 (26-04-28)** — 스타일 16종 (디즈니/픽사 포함) + 참고 이미지 드래그 앤 드롭 + 결과 카드 사용자 입력 자리 파란색 하이라이트 + 칩 그리드 정렬 (1·2·3번).

상세는 `FEATURES.md` §1, §2, §8.

---

## v0.5 (다음 릴리즈 후보)

- **P1** 프롬프트 즐겨찾기 — 자주 쓰는 입력 조합(작업유형 + 스타일 + 비율 + 금지 + 한·영 요청)을 이름 붙여 저장/불러오기. localStorage 기반.
- **P1** 결과 비교 모드 — 두 모델/버전의 결과를 좌우로 나란히 비교. 현재는 세로 스택만 가능.
- **P2** Niji 캐릭터 키워드 자동 분리 — 사양(`model-specs/NIJI.md`)이 권장하는 외형/표정/포즈/의상/배경/화각/매체 7항목을 별도 입력칸 또는 자동 재구성으로 처리. 현재는 사용자 영어 입력에 의존.
- **P2** 다크 모드 — 게임 디자이너 야간 작업 빈도 높음. Tailwind `dark:` 적용.

## v0.6 이후

- **P2** 실제 이미지 생성 API 연동 — 1차 정책 해제 시점에 결정. 진행 시 `SECURITY.md` §4 적용.
- **P2** 도구별 추천 프롬프트 예시 갤러리 — 학습/온보딩용.
- **P3** 다국어 UI (영/일).

---

## 작업 흐름

1. ROADMAP에서 항목 선택 → 작업 시작 (TodoList 생성).
2. 코드·문서 동시 갱신:
   - 코드 → `app/page.tsx` 또는 `lib/promptBuilder.ts`
   - 문서 → `FEATURES.md` 상태 변경 + 필요 시 design-docs / model-specs 갱신
3. 완료 → 이 파일에서 항목 제거 + `git push` (Vercel 자동 배포).
4. 보류 결정 → `BACKLOG.md`로 이동하면서 사유 기록.
5. 큰 변경은 DevSync 측 `CHANGELOG.md`에도 1~3줄 추가 검토.

---

## 우선순위 정의

- **P0** 긴급 / 라이브 사이트 장애
- **P1** 다음 릴리즈 포함 후보
- **P2** 언젠가 검토
- **P3** 가능성만 검토

P0가 1~2개를 넘기지 않게 관리.
