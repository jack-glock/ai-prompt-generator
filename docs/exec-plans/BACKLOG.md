# BACKLOG

> 안 한 일 + 사유. 다음 AI가 같은 고민 반복 방지용.
> `P0`(긴급) · `P1`(곧) · `P2`(언젠가) · `P-`(영구 제외)

## ~~P- 자동 번역 API~~ → v0.7에서 채택

- 26-04-29 v0.7에서 Gemini 2.5 Flash로 채택. **사용자 수동 클릭** 방식 (자동 실행 X).
- 무료 티어로 충분 (1회 ~1원). 키 보안: 서버 환경변수만, 클라이언트 미노출.

## P- 사용자 인증 / DB

1인 사용 도구. 즐겨찾기는 localStorage로 충분.

## P1 프롬프트 즐겨찾기 (localStorage)

입력 조합 이름 붙여 저장/불러오기. v0.8 후보.

## P1 결과 비교 모드 (좌우 분할)

두 모델/버전 좌우 비교. 현재 세로 스택만. v0.8 후보.

## P2 AI 분석 결과 카운트 표시

분석 후 "N개 옵션 채움 (작업유형, 성별, 나이대 등)" 같이 사용자가 즉각 인지하도록.
- v0.7에서 옵션 그룹 펼쳐서 확인하라는 안내까지 도달.
- 다음 단계: 채워진 슬롯 갯수를 메시지에 명시.

## P2 추천 프롬프트 갤러리

도구별 잘 나온 예시 모음. 클릭 시 자동 채움. 데이터 수집 필요.

## P2 이미지 생성 API + 미리보기

본 도구는 프롬프트 생성에 집중. 외부 사용자 요청 누적 시 재검토.

## P3 외부 이미지 호스팅 자동 업로드

MJ/Niji `--sref / --oref / --cref`에 실제 URL 자동 채우기. 현재는 사용자가 Discord에 직접 업로드 후 추가.

## P3 다국어 UI (영/일)

1인 한국어 사용. 외부 사용자 요청 누적 시 재검토.

---

**완료 이력**

- ✅ v0.5 — 다크 모드. Niji 7항목 분리.
- ✅ v0.6 — 작업 유형 5종, 옵션 시스템, 한글 격리, 키워드 매칭, 17종 스타일, 옵션 그룹 토글.
- ✅ v0.7 — Gemini API 연동 (번역·옵션·이미지 분석). Nano Banana 자연어형. MJ/Niji 키워드 단순화. UX 정비.
